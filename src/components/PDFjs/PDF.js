import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const PDF = () => {
  return <Container>PDF Functional Component</Container>;
};

export default PDF;
