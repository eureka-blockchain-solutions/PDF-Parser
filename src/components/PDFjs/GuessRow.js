import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const GuessRow = () => {
  return <Container>GuessRow Functional Component</Container>;
};

export default GuessRow;
